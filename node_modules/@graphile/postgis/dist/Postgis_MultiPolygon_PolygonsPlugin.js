"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("./constants");
const utils_1 = require("./utils");
const plugin = builder => {
    builder.hook("GraphQLObjectType:fields", (fields, build, context) => {
        const { scope: { isPgGISType, pgGISType, pgGISTypeDetails }, } = context;
        if (!isPgGISType ||
            !pgGISTypeDetails ||
            pgGISTypeDetails.subtype !== constants_1.GIS_SUBTYPE.MultiPolygon) {
            return fields;
        }
        const { extend, getPostgisTypeByGeometryType, graphql: { GraphQLList }, } = build;
        const { hasZ, hasM, srid } = pgGISTypeDetails;
        const Polygon = getPostgisTypeByGeometryType(pgGISType, constants_1.GIS_SUBTYPE.Polygon, hasZ, hasM, srid);
        return extend(fields, {
            polygons: {
                type: new GraphQLList(Polygon),
                resolve(data) {
                    return data.__geojson.coordinates.map((coord) => ({
                        __gisType: utils_1.getGISTypeName(constants_1.GIS_SUBTYPE.Polygon, hasZ, hasM),
                        __srid: data.__srid,
                        __geojson: {
                            type: "Polygon",
                            coordinates: coord,
                        },
                    }));
                },
            },
        });
    });
};
exports.default = plugin;
//# sourceMappingURL=Postgis_MultiPolygon_PolygonsPlugin.js.map